# Python packages for generating the SocietyEase diagrams.
#   pip install -r requirements.txt
#
# NOTE: the `graphviz` Python package is only a thin wrapper around the
# Graphviz `dot` system binary, which must be installed separately:
#   Windows:  winget install Graphviz.Graphviz   (or  choco install graphviz)
#   macOS:    brew install graphviz
#   Debian:   sudo apt-get install graphviz
graphviz>=0.20
matplotlib>=3.8
